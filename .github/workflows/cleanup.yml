name: D1 Garbage Collection

on:
  schedule:
    - cron: '0 3 * * *'   # 每日 UTC 03:00 (台灣時間 11:00)
  workflow_dispatch:        # 允許手動觸發

jobs:
  cleanup:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Delete expired auth_codes
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          command: d1 execute chiyigo_db --remote --command "DELETE FROM auth_codes WHERE expires_at < datetime('now');"

      - name: Delete expired pkce_sessions
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          command: d1 execute chiyigo_db --remote --command "DELETE FROM pkce_sessions WHERE expires_at < datetime('now');"

      - name: Delete expired refresh_tokens
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          command: d1 execute chiyigo_db --remote --command "DELETE FROM refresh_tokens WHERE expires_at < datetime('now');"

      - name: Delete expired email_verifications
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          command: d1 execute chiyigo_db --remote --command "DELETE FROM email_verifications WHERE expires_at < datetime('now');"

      - name: Delete expired oauth_states
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          command: d1 execute chiyigo_db --remote --command "DELETE FROM oauth_states WHERE expires_at < datetime('now');"

      - name: Delete expired password_resets (legacy)
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          command: d1 execute chiyigo_db --remote --command "DELETE FROM password_resets WHERE expires_at < datetime('now');"

      - name: Delete stale login_attempts (>1 day)
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          command: d1 execute chiyigo_db --remote --command "DELETE FROM login_attempts WHERE created_at < datetime('now', '-1 day');"
